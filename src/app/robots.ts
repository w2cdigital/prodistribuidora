/* import type { MetadataRoute } from "next"

export default function robots(): MetadataRoute.Robots {
  return {
    sitemap: "https://grupomedtech.com.br/sitemap.xml",
    rules: {
      userAgent: "*",
      allow: "/",
      disallow: ["/Admin", "/Admin*"],
    },
  }
}
 */

import styled, { css } from "styled-components"

export const Wrapper = styled.div(
  () => css`
    width: 2.5rem;
    height: 2.5rem;
    margin-left: auto;
    display: flex;
    justify-content: center;
    align-items: center;
    cursor: pointer;
  `,
)
